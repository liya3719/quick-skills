# 技术方案 overlay（存量模块新增功能）

## 何时加载

- Step 1 写入存量理解时
- Step 3 落盘《技术方案》时**必读**（无论是否安装 `quick-tech-solution`）

## 与 quick-tech-solution 的关系

| 环境 | 正文结构 | 本 overlay |
|------|----------|--------------|
| **已安装** | 按 `quick-tech-solution` 的 `document-outline.md` | **追加**下列章节，不得省略 |
| **未安装** | 自建 Markdown，须含 overlay 全部章节 + REQ 追溯 | 即最低结构约束 |

## 必含章节（overlay）

### 范围卡片
> 结构见 `understand-call-chain.md`；Step 0 起写入 `{artifactRoot}/技术方案.md`

### §1 原模块职责复述
- 一段话：职责、不变量、对外契约

### §2 上下游调用链与影响范围
- Mermaid + 文字；复杂时加载 `影响范围模板.md`
- **不应受影响的路径**（显式列出）

### §5 最小改动证据
| 文件 | 改动类型 | 改动原因 | 对应 REQ/切片 | 是否可避免 |
|---|---|---|---|---|
|  |  |  |  |  |

须与《实施计划》切片预对齐；终版与 git diff 一致。

### §8 测试映射（对接三向追溯矩阵）
| REQ | 设计要点 | 研发 TC | QA TC/RG |
|---|---|---|---|
| REQ-001 |  | RD-TC-01 | QA-TC-01 |

### 附录 A：三向追溯矩阵
> 见 `三向追溯矩阵.md`；可与《需求拆解》末尾合并

## 设计正文约束

- 每个 REQ 在设计正文有锚点（小节标题或表格列 REQ-xxx）
- 改既有条件、默认值、错误语义：写明理由 + 兼容策略 + 同步测试映射
- 无定量 NFR 时显式写「无定量 NFR」+ 工程补充默认

## 禁止

- 仅复述 PRD、无模块边界与挂载点
- 遗漏「不应受影响」路径
- §5 / §8 留空或与技术方案正文 REQ 编号不一致
